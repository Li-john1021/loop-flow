# loop-flow validate

只读运行可用的 Schema、引用、批注、依赖、Review 顺序和指纹检查。优先使用：`python scripts/validate.py --root <loop-flow-root> --require-jsonschema`。缺少 Python 或 `jsonschema` 时报告结构化降级，不得声称完整校验通过。

参数：`{{ARGS}}`
